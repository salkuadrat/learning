name: learning_entity_extraction_example
description: Demonstrates how to use the learning_entity_extraction plugin.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  learning_input_image:
    path: ../../learning_input_image
  learning_entity_extraction:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
