/* import 'package:learning_input_image/input_image.dart';
import 'package:learning_text_recognition/src/text_recognition.dart';

import 'text.dart';

class MLTextRecognition {
  static Future<MLText?> process(InputImage image) async {
    TextRecognition textRecognition = TextRecognition();
    final result = await textRecognition.process(image);
    await textRecognition.dispose();
    return result;
  }
} */
