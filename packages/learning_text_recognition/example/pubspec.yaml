name: learning_text_recognition_example
description: Demonstrates how to use the learning_text_recognition plugin.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  
  provider: ^5.0.0
  learning_input_image: ^0.0.5
  learning_text_recognition:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
