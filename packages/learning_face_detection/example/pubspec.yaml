name: learning_face_detection_example
description: Demonstrates how to use the learning_face_detection plugin.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  
  provider: ^5.0.0
  learning_input_image: ^0.0.5
  learning_face_detection: ^0.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
